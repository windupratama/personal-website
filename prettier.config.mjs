/**
 * @type {import('prettier').Config}
 */
const config = {
    tabWidth: 4,
    semi: true,
    trailingComma: "all",
    quoteProps: "preserve",
    plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
